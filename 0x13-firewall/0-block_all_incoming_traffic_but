sudo apt-get update

sudo apt-get install -y ufw

sudo ufw default deny incoming

sudo ufw default allow outgoing

sudo ufw allow ssh

sudo ufw allow 22

sudo ufw allow 80/tcp #Accept Nginx

sudo ufw allow 443/tcp #Accept HTTP connections

sudo ufw enable
