#!/usr/bin/env bash
# Script that generates a MySQL dump and creates a compressed archive out of it

MYSQL_PASSWORD="$1"

mysqldump -uroot -p"$MYSQL_PASSWORD" --all-databases | gzip > backup.sql.gz

tar -cvzf "$(date +%Y-%m-%d)".tar.gz backup.sql.gz
